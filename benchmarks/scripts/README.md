# Benchmark scripts

| Script | Purpose |
|--------|---------|
| `run_anla_ablation.py` | ANLA on vs off counts on `datasets/ablation_prompts.json` |

```bash
pip install -e ".[dev]"
python benchmarks/scripts/run_anla_ablation.py
```