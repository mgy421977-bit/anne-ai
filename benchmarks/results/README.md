# Benchmark results

Committed JSON artifacts from controlled runs. Each file should state fixture version, timestamp, and an explicit **non-claim** note.

See [RESULTS.md](RESULTS.md) for the human-readable summary.

Re-run:

```bash
python benchmarks/scripts/run_anla_ablation.py
```